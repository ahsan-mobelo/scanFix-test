/**
 * Deliberate hardcoded token string for scanner tests (not a real credential).
 * Avoid Stripe/AWS-shaped literals here — GitHub push protection blocks those even when fake.
 */
export const PLACEHOLDER_SCANFIX_TEST_TOKEN =
  "scanfix-demo-hardcoded-token-not-for-production";
